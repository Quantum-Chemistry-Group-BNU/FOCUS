#include "tns_decimation.h"

using namespace std;
using namespace tns;
using namespace linalg;

qtensor3 tns::decimation_onedot(const comb& icomb,
		 	        const comb_coord& p,
			        qtensor3& wf,
			        matrix& vsol){
   cout << "tns::decimation_onedot" << endl;
   // pRDM
   // decimation
   return wf;
}
