#ifndef CTNS_LASTDOT_H
#define CTNS_LASTDOT_H

namespace ctns{

   template <typename Qm, typename Tm>
   void rcanon_lastdot(comb<Qm,Tm>& icomb){

      std::cout << "error: not implemented yet" << std::endl;
      exit(1);

   }

} // ctns

#endif
