
To use these functions, add path to PYTHONPATH:

   export PYTHONPATH="/Users/zhendongli/Desktop/FOCUS/pyutils:${PYTHONPATH}"

Example for input for prop.x

   $ctns
   verbose 1
   qkind rNSz
   topology_file topo
   rcanon_file rcanon_isweep1
   iroot 0
   savebin
   task_prop 2p2h
   $end
